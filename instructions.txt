
### SETUP ###
note: Terminal = OS Terminal, not the one in PyCharm


Run in Terminal

    . ~/.virtualenv/env/bin/activate
    cd PycharmProjects/silisili-web/
    pip install -r requirements.txt

In silisili/settings.py, change to the following

    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.sqlite3',
            'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
            # 'ENGINE': 'django.db.backends.postgresql_psycopg2',
            # 'NAME': 'postgres',
            # 'USER': 'dk',
            # 'PASSWORD': '1',
            # 'HOST': '127.0.0.1',
            # 'PORT': '5432',
        }
    }

Run this line in the Terminal:
    python manage.py migrate



### Optional (for first time only) ###

Create superuser (admin), run this line and enter the information (can skip email)
    python manage.py createsuperuser

Go to localhost:8000/admin

Find 'Sites' on the screen after login, go into that and change the existed one to:
    localhost:8000
    SiliSili (optional)

Back to admin homepage, find 'Social applications' then add two new:
        Google App,
            Client id = 30365204598-vsk8l9ebs3tes590tgn3lf37ji3vl2bb.apps.googleusercontent.com,
            Secret key = aje8P3fL8BBENZqpGKAthSyK
        FB App
            Client id = 1735709893344808
            Secret key = a764d464bd8609806cf0d93275007bf0


